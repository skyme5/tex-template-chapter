\documentclass[]{scrbook}

\usepackage{tikz}
\usepackage{tikzpagenodes}

\begin{document}
	
	\begin{tikzpicture}[remember picture,overlay]
	(current page.west)  (current page.east);
	\node[text width=.5\paperwidth,minimum height=6cm,minimum width=.5\paperwidth] at 
	(current page.west, current page.north) {some text spanning three lines with automatic line breaks};
	\end{tikzpicture}
	
\end{document}

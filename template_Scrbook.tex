\documentclass[]{scrbook}

\usepackage{tikz}
\usepackage{tikzpagenodes}

\begin{document}
	
	\begin{tikzpicture}
	(current page.west) rectangle (current page.east);
	\node[draw,text width=.5\paperwidth,minimum height=6cm,minimum width=.5\paperwidth] at 
	(current page text area.west|-current page.north) {some text spanning three lines with automatic line breaks};
	\end{tikzpicture}
	
\end{document}
